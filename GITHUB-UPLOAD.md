# GitHub-Upload: autohaus-dashboard

Dieses Projekt ist vorbereitet fuer das Repository `autohaus-dashboard`.

## Variante A: GitHub Desktop

1. GitHub Desktop oeffnen.
2. `File` > `Add Local Repository...`
3. Diesen Ordner auswaehlen:
   `/Users/andreashauser/Documents/New project 8`
4. Falls GitHub Desktop fragt: `Create a repository`.
5. Repository-Name: `autohaus-dashboard`
6. Commit erstellen.
7. `Publish repository` auswaehlen.

## Variante B: Terminal

```bash
cd "/Users/andreashauser/Documents/New project 8"
git init -b main
git add .
git commit -m "Initial autohaus dashboard"
git remote add origin https://github.com/DEIN-GITHUB-NAME/autohaus-dashboard.git
git push -u origin main
```

## Hinweis

In Codex ist aktuell kein GitHub-Konto/Repository sichtbar. Sobald GitHub verbunden ist oder die vollstaendige Repository-Adresse bekannt ist, kann der Stand direkt hochgeladen werden.
